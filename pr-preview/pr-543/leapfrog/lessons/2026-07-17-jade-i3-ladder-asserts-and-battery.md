# 2026-07-17 — Jade AAA I3: ladder asserts encode the BLOSSOMING verb; the coexist proof at scale

**Did / learned.** Increment 3 — locked the AAA ladder into `tests/starters.mjs` as permanent
identity asserts and ran the full verification battery. The jade block now gates (12 new asserts):
`crestRibbon` / `caudalBloom` / `lobeBreath` / `bodyArcY` / `bodyWaveAmpY` all monotonic ↑ across
the three forms; the pearl-chain **link count rising to exactly 5 at apex** (pearl + dew + satellite
+ lyre + streamer) and 0 tickable chain mats below (the bead's own mat is unpublished under
pearlStage 2 — shipped); the lyre gems present as exactly **2 waveRiders at apex, none at f0**; the
fever fields present (`feverEye`/`feverWing`/`feverWash`); and — folded back in as regression
guards — **B3** (every apex spineMat userData-stamped) and **B1** (the pearl-chain plumbing
forwarded through the PREVIEW return too, the exact drop the v2 audit caught).

**Verified (the full §8 battery):** starters **505/0** (was 493 — +12 jade asserts); tricount
2510/4458/5396 monotonic, <6000, inside the re-pinned bands; **run-all: every real suite green**
(the lone `FAILED: _diag-rock-caps.mjs` is a canyon-rock diagnostic that fails STANDALONE on master
too — proven by stash — and run-all is not a CI gate here; the PR's only check is the preview
deploy). blueprint / smoke / slither / seamprobe / wingsymprobe all green or baseline-identical.

**→ Systematize.** (1) **A ladder assert should encode the VERB, not just the budget.** Jade's verb
is BLOSSOMING — so the asserts are "every growth dial monotonic ↑" + "the chain reaches its 5-link
coronation only at apex" + "the rider count is apex-gated", which is what makes each rung a
*category add* and not same-dragon-bigger. Budget (tris) is necessary but says nothing about whether
the dragon earns its rungs. (2) **Fold the audit's bug-classes into the ship test as regression
guards** — B1 (both-returns forwarding) and B3 (userData stamps) are now asserts, so the dead-code
drop and the white-@1.0 bug cannot silently return. (3) **When a full-suite run shows one red,
identify WHETHER it's yours before treating it as a blocker** — a `_diag`-prefixed standalone
diagnostic reading NaN from "0 samples" in isolation is environmental noise; stash-and-run on master
to confirm, and check whether CI even gates on it (here it doesn't).

**→ Leapfrog.** The build is headless-complete and gated by machine numbers + a fresh harsh Fable
critic on the rear-chase captures. Two things remain HUMAN-only by construction and go to the owner
on the PR: (a) the caudal-fan silhouette GROWTH sign-off (§3a.7 — measured, not self-sanctioned),
and (b) the motion/feel the stills can't show — chain-travel speed, the fan breathing open, and the
first-ever Surge mint (jade's pearl-family emissive has literally never rendered in-game before this
build, because of the B1 forwarding drop). The next dragon inherits the pearl-chain kit + the
waveRiders hook + the both-returns forwarding assert as reusable systems.
