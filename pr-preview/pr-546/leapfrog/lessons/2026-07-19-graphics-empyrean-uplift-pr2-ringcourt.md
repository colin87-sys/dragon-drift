
**Gate record (final).** R1 4.0 (arch=mound) → R2 4.1 (arch=blade; picket+drift fixed) → R3 4.1 (arch
geometry right but NEVER DEMONSTRATED — random yaw + band-edge capture hid every aperture) → **R4 4.4
PASS** ("honest daylight under its horns"). Two compounding lessons: (a) the depth-projection trap
applies to PROPS — broadside-bias any aperture-identity archetype (`def.broadside`, a pure yaw remap
after the rnd init); (b) **the capture must respect the band map it audits** — the early shot at d=1200
looked AHEAD across the 0.34 cutoff where arches park, so three rounds judged frames that structurally
could not contain the thing under test. Place gate shots at an in-band congregation peak (d≈380).
