# EMPYREAN uplift PR-B — STONES OF LIGHT + WATER CONSISTENCY (2 rounds)

**What shipped** (rides the `empyStruct` gate; kills 5.5-review gaps 3+5): the `_bakeRamp` ladder now
carries a HUE ramp (violet base → pearl body → rose-tinged crown) across every biome-5 prop — one
coherent kit; a crown-FEATHER band (top 18% leans rose) so the cap reads as light catching the crown,
not a paint line; TWO-TONE corridor faces (owner amendment #2: faces toward the lane +22%, away −8%,
flat-face normals from screen derivatives — implies the bright corridor, never a sun); the water
quilt killed by a distance LOD-COLLAPSE; ramp mottle halved. Gate: 3.8 → **4.3 PASS**.

**Lessons.**
- **A domain warp WOBBLES a constant-frequency lattice; it cannot kill it.** The quilt read comes from
  interference CONTRAST, not alignment — the fix is a distance-driven LOD-collapse of the deep/shallow
  mix contrast (`h`'s contribution → 0 beyond mid-range) so the far field resolves to smooth pearl.
  Texture belongs near-field; the horizon belongs to the gradient.
- **Noise drowns hue.** The ramp's erosion mottle (±5.5%) was larger than the hue separation it sat on
  — "violet base" read as "noisy grey". Halve the noise BEFORE deepening the hue; then a modest hue
  ramp reads. (Same ACES family as PR-1: subtle structure must not compete with its own texture.)
- **A hard mat-boundary can be feathered from the BODY side** — the crown band leaning rose makes the
  geometric body/crown split read as a lit gradient without touching the 2-mat budget.
- **Probe sample-bands have geometry too:** movingRose collapsed to 109 purely because the ring's arc
  rides the probe's y-cutoff at some pulse phases — high variance in a probe = check the probe's
  window before suspecting the art (the ring was pixel-confirmed by the PR-A gate).

**Deferred nits (sub-threshold, logged):** faint mid-range dapple regularity in the late shot; stump
caps read paint-dipped at small screen size (the feather band needs more pixels than a stump has).
